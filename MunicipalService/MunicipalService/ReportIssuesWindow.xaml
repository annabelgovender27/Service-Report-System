<Window x:Class="MunicipalService.ReportIssuesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MunicipalService"
        mc:Ignorable="d"
        Title="Report an Issue"
        Height="750"
        Width="1100"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F8FDF9">

    <Window.Resources>
        <!-- Green Color Palette -->
        <Color x:Key="PrimaryColor">#2E7D32</Color>
        <Color x:Key="PrimaryDarkColor">#1B5E20</Color>
        <Color x:Key="AccentColor">#4CAF50</Color>
        <Color x:Key="LightBackground">#F8FDF9</Color>
        <Color x:Key="CardBackground">#FFFFFF</Color>
        <Color x:Key="HoverColor">#E8F5E9</Color>

        <!-- Convert Colors to Brushes -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="LightBackgroundBrush" Color="{StaticResource LightBackground}"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackground}"/>
        <SolidColorBrush x:Key="HoverBrush" Color="{StaticResource HoverColor}"/>

        <!-- Back Button Style -->
        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Outline Button Style -->
        <Style x:Key="OutlineButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"
                                          Background="Transparent"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="36"/>
        </Style>

        <!-- Section Header Style -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- Form Label Style -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <!-- Required Field Style -->
        <Style x:Key="RequiredLabel" TargetType="TextBlock" BasedOn="{StaticResource FormLabel}">
            <Setter Property="Text" Value="*"/>
            <Setter Property="Foreground" Value="#D32F2F"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="2,0,0,0"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource LightBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Back Button -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="10">
            <DockPanel>
                <Button x:Name="BackButton"
                        Content="← Back to Main Menu"
                        DockPanel.Dock="Left"
                        Click="BackButton_Click"
                        Width="140"
                        Style="{StaticResource BackButtonStyle}"/>
                <TextBlock Text="Report a Municipal Issue"
                           Foreground="White"
                           FontSize="18"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <!-- Main Form Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30">
                <!-- Form Card -->
                <Border Background="{StaticResource CardBackgroundBrush}" 
                        CornerRadius="12" 
                        Padding="25"
                        BorderBrush="#E8E8E8"
                        BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Report an Issue" 
                                   Style="{StaticResource SectionHeader}"
                                   Margin="0,0,0,20"/>

                        <!-- Location Input -->
                        <StackPanel Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Location of the Issue (Street Address)" 
                                           Style="{StaticResource FormLabel}"/>
                                <TextBlock Style="{StaticResource RequiredLabel}"/>
                            </StackPanel>
                            <TextBox x:Name="LocationTextBox"
                                     Style="{StaticResource ModernTextBox}"
                                     AcceptsReturn="True"
                                     Height="80"
                                     VerticalScrollBarVisibility="Auto"
                                     TextWrapping="Wrap"
                                     ToolTip="Please provide the full address with street name, suburb, and city"/>
                        </StackPanel>

                        <!-- Area Selection -->
                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Area" 
               FontWeight="SemiBold" 
               FontSize="13"
               Foreground="{StaticResource PrimaryDarkBrush}"
               Margin="0,0,0,6"/>
                            <ComboBox x:Name="AreaComboBox" 
              Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Durban"/>
                                <ComboBoxItem Content="Pinetown"/>
                                <ComboBoxItem Content="Hillcrest"/>
                                <ComboBoxItem Content="Kloof"/>
                                <ComboBoxItem Content="New Germany"/>
                                <ComboBoxItem Content="Umhlanga"/>
                                <ComboBoxItem Content="Amanzimtoti"/>
                                <ComboBoxItem Content="Westville"/>
                                <ComboBoxItem Content="Ballito"/>
                                <ComboBoxItem Content="Umbilo"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Category Selection -->
                        <StackPanel Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Category" 
                                           Style="{StaticResource FormLabel}"/>
                                <TextBlock Style="{StaticResource RequiredLabel}"/>
                            </StackPanel>
                            <ComboBox x:Name="CategoryComboBox"
                                      Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem>Sanitation</ComboBoxItem>
                                <ComboBoxItem>Roads &amp; Potholes</ComboBoxItem>
                                <ComboBoxItem>Water &amp; Utilities</ComboBoxItem>
                                <ComboBoxItem>Electricity</ComboBoxItem>
                                <ComboBoxItem>Parks &amp; Recreation</ComboBoxItem>
                                <ComboBoxItem>Other</ComboBoxItem>
                            </ComboBox>
                        </StackPanel>

                        <!-- Description -->
                        <StackPanel Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Description" 
                                           Style="{StaticResource FormLabel}"/>
                                <TextBlock Style="{StaticResource RequiredLabel}"/>
                            </StackPanel>
                            <TextBox x:Name="DescriptionTextBox"
                                     Style="{StaticResource ModernTextBox}"
                                     AcceptsReturn="True"
                                     Height="100"
                                     VerticalScrollBarVisibility="Auto"
                                     TextWrapping="Wrap"
                                     ToolTip="Please describe the issue in detail..."/>
                        </StackPanel>

                       

                        <!-- Photo Attachment -->
                        <StackPanel Margin="0,0,0,25">
                            <TextBlock Text="Attach a Photo (Optional)" 
                                       Style="{StaticResource FormLabel}"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="FilePathTextBox"
                                         Grid.Column="0"
                                         Style="{StaticResource ModernTextBox}"
                                         IsReadOnly="True"
                                         Margin="0,0,10,0"
                                         ToolTip="No file selected"/>
                                <Button x:Name="BrowseButton"
                                        Grid.Column="1"
                                        Content="Browse..."
                                        Style="{StaticResource OutlineButton}"
                                        Width="100"
                                        Click="BrowseButton_Click"/>
                            </Grid>
                        </StackPanel>

                        <!-- Encouraging Message -->
                        <Border Background="#E8F5E9"
                                CornerRadius="8"
                                Padding="15"
                                BorderBrush="#4CAF50"
                                BorderThickness="1">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="🌟" 
                                           FontSize="16"
                                           VerticalAlignment="Center"
                                           Margin="0,0,8,0"/>
                                <TextBlock Text="Thank you for making our community better!" 
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource PrimaryDarkBrush}"
                                           TextAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Submit Button -->
        <Border Grid.Row="2" 
                Background="{StaticResource CardBackgroundBrush}" 
                Padding="20" 
                BorderThickness="1,0,0,0" 
                BorderBrush="#E8E8E8">
            <Button x:Name="SubmitButton"
                    Content="Submit Report"
                    Style="{StaticResource ModernButton}"
                    Width="140"
                    HorizontalAlignment="Center"
                    FontSize="14"
                    Click="SubmitButton_Click"/>
        </Border>
    </Grid>
</Window>